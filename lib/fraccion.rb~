require "gcd.rb"

class Fraccion
  
  attr_reader :num, :den
  
  def initialize (num, den)
    @num, @den = num, den
  end
  
  def reduccion (fracc)
    divisor = gcd(fracc.num, fracc.den)
    fracc2 = Fraccion.new(fracc.num/divisor, fracc.den/divisor)
    fracc2
  end
  
end